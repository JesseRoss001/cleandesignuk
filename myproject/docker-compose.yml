version: '3.8'

services:
  web:
    build: .
    command: gunicorn myproject.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/workspace
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DJANGO_SETTINGS_MODULE=myproject.settings
    depends_on:
      - db

  db:
    image: postgres:13
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=defaultdb
      - POSTGRES_USER=doadmin
      - POSTGRES_PASSWORD=AVNS_B2GpkX922z4qvlPnr95
      - POSTGRES_HOST=db-postgresql-lon1-23103-do-user-16160735-0.c.db.ondigitalocean.com
      - POSTGRES_PORT=25060

volumes:
  postgres_data:
